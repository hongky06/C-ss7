#include<stdio.h>
int main(){
	int arr[5]={1,2,3,4,6};
	if (arr[0] % 2 == 0 ){
		printf("\n so chan co trong day la %d",arr[0]);
    }else {
	}
	if (arr[1] % 2 == 0 ){
		printf("\n so chan co trong day la %d",arr[1]);
	}else{
	}
	
	if (arr[2] % 2 == 0 ){
		printf("\n so chan co trong day la %d",arr[2]);
	}else{
	}
	 
	if (arr[3] % 2 == 0 ){
	printf("\n so chan co trong day la %d",arr[3]);
	}else{
	}
	if (arr[4] % 2 == 0 ){
		printf("\n so chan co trong day la %d",arr[4]);
		
	}else{
	}
	 
	
	
	return 0;
}
